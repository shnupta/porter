[instance]
name = "porter-home"
port = 3101
db_path = "porter-home.db"

[integrations]
enabled = ["tasks"]

[agents]
claude_binary = "claude"
max_concurrent_sessions = 3
default_model = "opus"

# MCP servers available to Claude agent sessions.
# Add new external integrations here — no Rust code needed.

[agents.mcp.fetch]
command = "npx"
args = ["-y", "@anthropic/mcp-server-fetch"]

# [agents.mcp.dining]
# command = "npx"
# args = ["-y", "opentable-mcp-server"]
# env = { OPENTABLE_API_KEY = "env:PORTER_OPENTABLE_KEY" }
#
# [agents.mcp.flights]
# command = "npx"
# args = ["-y", "google-flights-mcp"]
#
# [agents.mcp.slack]
# command = "npx"
# args = ["-y", "@anthropic/slack-mcp"]
# env = { SLACK_TOKEN = "env:PORTER_SLACK_TOKEN" }
